name: pettime_frontend
description: Projeto Flutter do PETTIME

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mask_text_input_formatter: ^2.4.0
  shared_preferences: ^2.2.2
  image_picker: ^1.0.4
  # flutter_local_notifications: ^17.2.3  # Removido - usando sistema interno
  cupertino_icons: ^1.0.2
  http: ^1.4.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true